"use client";

// RecentProjects removed from the UI in this branch; keep a harmless stub
// to avoid import errors in case other files still import it.
const RecentProjects = () => null;

export default RecentProjects;
